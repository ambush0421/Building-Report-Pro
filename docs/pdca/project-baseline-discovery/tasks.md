# Tasks: Project Baseline Discovery

1. [x] 프로젝트 루트/핵심 파일(`src/app`, `src/app/api`, `src/lib`, `src/stores`)을 스캔해 현재 구조를 파악한다.
2. [x] 기존 PDCA 산출물(`docs/pdca/*`)과 완료 상태를 점검한다.
3. [x] 본 feature의 `plan.md`를 생성한다.
4. [x] 본 feature의 `design.md`를 생성한다.
5. [x] 실제 온보딩에 필요한 항목(라우트/API/환경변수/스크립트)으로 `README.md`를 업데이트한다.
6. [x] lint 검증을 수행하고 결과를 수집한다. (`npx eslint src`, `npm run lint`)
7. [x] 본 feature의 `report.md`를 작성한다.
